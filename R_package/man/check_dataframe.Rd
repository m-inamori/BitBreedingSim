% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Population.R
\name{check_dataframe}
\alias{check_dataframe}
\title{Validate the structure and content of the data frame used for crossing information}
\usage{
check_dataframe(df)
}
\arguments{
\item{df}{A data frame. Must include the following columns:
- \code{mat}: Names of the maternal parents for each cross.
- \code{pat}: Names of the paternal parents for each cross.
- \code{num}: Number of offspring to generate for each cross. Must contain positive numeric values.}
}
\value{
No return value. If the validation is successful, the function proceeds silently.
If the validation fails, the function stops execution and displays an error message.
}
\description{
This function checks if the input data frame \code{df} meets the required
structure for the \code{cross_by_table} function. It ensures that \code{df} is a valid
\code{data.frame}, contains the required columns (\code{mat}, \code{pat}, \code{num}),
and verifies that the \code{num} column has positive numeric values.
If any of these checks fail, the function stops execution and displays
an error message.
}
\examples{
# Valid input example
df <- data.frame(mat = c("mat1", "mat2"), pat = c("pat1", "pat2"), num = c(1, 2))
check_dataframe(df) # Passes silently

# Invalid input example: Missing 'num' column
df_invalid <- data.frame(mat = c("mat1", "mat2"), pat = c("pat1", "pat2"))
check_dataframe(df_invalid) # Throws an error
}
